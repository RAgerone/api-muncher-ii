<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Yum Yum</title>
  </head>
  <body>
    <%# Show all of the fancy recipes here %>
    <% @recipes.each do |recipe| %>
    <section class="small-2 large-4 columns">
      <h3><%= link_to recipe.name, recipe_path(recipe.uri) %></h3>
      <% recipe.image %>
      <img src="<%=recipe.image %>" alt=<%= recipe.name %>>
      <% end %>

    </section>
  </body>
</html>
